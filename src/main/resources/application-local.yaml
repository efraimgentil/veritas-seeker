
spring:
  rabbitmq:
    username: rabbit_user
    password: rabbit_pass
  datasource:
    url: jdbc:postgresql://localhost:5432/seeker?ApplicationName=${spring.application.name}
    username: seeker
    password: postgres
  liquibase:
    user:     seeker
    password: postgres
  jpa:
    show-sql: true


feign:
  client:
    config:
      dadosAbertosClient:
        loggerLevel: FULL

logging.level.me.efraimgentil.seeker.client.DadosAbertosClient: DEBUG